# Development requirements for the desktop worker wrapper.
# The core separation engine is distributed as the PyPI package `pymss`.
# Install it with: python -m pip install pymss
#
# This file documents the runtime modules the worker expects to be available
# in the Python interpreter selected by PYMSS_STUDIO_PYTHON or by "python".

av>=14
librosa>=0.10.2
numpy>=1.26
pysocks>=1.7.1
requests[socks]>=2.32
pyyaml>=6.0.1
torch>=2.7.1
tqdm>=4.60
pymss
